{
  "$schema": "https://schema.railpack.com",
  "packages": {
    "node": "22"
  },
  "steps": {
    "install-claude-cli": {
      "commands": [
        "npm install -g @anthropic-ai/claude-code",
        "ln -sf /mise/installs/node/*/bin/claude /usr/local/bin/claude"
      ]
    }
  }
}
